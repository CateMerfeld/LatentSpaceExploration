# LatentSpaceExploration
A repo to explore the structure and utility of latent spaces.

To install a jupyter kernel: python -m ipykernel install --user --name latentspaceexploration --display-name "Python (LatentSpaceExploration)"